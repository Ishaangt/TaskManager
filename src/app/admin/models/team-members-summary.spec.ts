import { TeamMembersSummary } from './team-members-summary';

describe('TeamMembersSummary', () => {
  it('should create an instance', () => {
    expect(new TeamMembersSummary()).toBeTruthy();
  });
});
